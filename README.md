# PARCS Parallel Brute-Force Password Cracker

Паралельна реалізація brute-force алгоритму для злому паролів з використанням PARCS-Python та Google Cloud Platform.

## 📋 Зміст

- [Опис проекту](#description)
- [Архітектура](#architecture)
- [Встановлення та налаштування](#installation-and-setup)
- [Використання](#usage)
- [Результати тестування](#test-results)
- [Аналіз продуктивності](#performance-analysis)
- [Технічний стек](#technical-stack)
- [Автор](#author)

## 🎯 Опис проекту <a id="description"></a>

Система реалізує паралельний brute-force алгоритм для відновлення паролів за MD5-хешем з використанням солі (salt). Проект демонструє ефективність розподілених обчислень на платформі PARCS для обчислювально-складних завдань.

### Особливості

- ✅ Паралельна обробка на декількох воркерах
- ✅ Підтримка довільної довжини паролів
- ✅ Використання MD5 хешування з сіллю
- ✅ Динамічний розподіл навантаження
- ✅ Розгортання на GCP з використанням Docker контейнерів

## 🏗️ Архітектура <a id="architecture"></a>

### Алгоритм роботи

1. **Визначення простору пошуку**: Використовується набір символів (a-z, 0-9), загальний простір = Base^Length
2. **Розподіл задач**: Простір пошуку ділиться між воркерами
3. **Генерація кандидатів**: Кожне число перетворюється на комбінацію символів
4. **Хешування та перевірка**: До кандидата додається сіль, обчислюється MD5-хеш та порівнюється з цільовим

### Компоненти

- **Master node**: Координує роботу та розподіляє завдання
- **Worker nodes**: Виконують brute-force перебір у виділених діапазонах
- **PARCS framework**: Забезпечує комунікацію через Pyro4

## 🚀 Встановлення та налаштування <a id="installation-and-setup"></a>

### Передумови

- Google Cloud SDK
- Docker
- Python 3.x з бібліотекою Pyro4

### Налаштування GCP

#### 1. Створення та конфігурація проекту

```bash
# Створення нового проекту
gcloud projects create parallel-brute-force

# Встановлення активного проекту
gcloud config set project parallel-brute-force

# Налаштування зони та регіону
gcloud config set compute/zone europe-north1-a
gcloud config set compute/region europe-north1
```

#### 2. Налаштування firewall правил

```bash
# Дозволити весь вхідний трафік (для development)
gcloud compute firewall-rules create allow-all \
  --direction=INGRESS \
  --priority=1000 \
  --network=default \
  --action=ALLOW \
  --rules=all \
  --source-ranges=0.0.0.0/0
```

⚠️ **Увага**: Це правило відкриває всі порти. Для production використовуйте більш обмежені правила.

#### 3. Створення VM екземплярів

```bash
# Створення master ноди
gcloud compute instances create-with-container master \
  --container-image=registry.hub.docker.com/hummer12007/parcs-node \
  --container-env PARCS_ARGS="master"

# Отримання internal IP master ноди (наприклад, 10.166.0.4)
gcloud compute instances describe master --format='get(networkInterfaces[0].networkIP)'

# Створення worker нод
gcloud compute instances create-with-container worker1 worker2 worker3 \
  --container-image=registry.hub.docker.com/hummer12007/parcs-node \
  --container-env=PARCS_ARGS="worker 10.166.0.4"
```

#### 4. Налаштування master контейнера

```bash
# Підключення до master ноди
gcloud compute ssh master

# Зупинка та видалення контейнера за замовчуванням
docker stop $(docker ps -q)
docker rm parcs-master

# Запуск нового контейнера з відкритим портом
docker run -d -p 8080:8080 \
  --name parcs-master \
  -e PARCS_ARGS="master" \
  registry.hub.docker.com/hummer12007/parcs-node

# Перевірка статусу
docker ps
```

## 💻 Використання <a id="usage"></a>

### Формат вхідних даних (input.txt)

```
<MD5_hash>
<salt>
<password_length>
```

**Приклад для довжини 4**:
```
4a90ba043fe6ad366a4a0c0f4e18da59
ntw9lrwu
4
```

**Приклад для довжини 5**:
```
9533acbe8a80d501d367c6ee8f1ac0fc
hykjpxhc
5
```

## 📊 Результати тестування <a id="test-results"></a>

### Тест 1: Пароль довжиною 4 символи

| Воркери | Час (с) | Прискорення | Ефективність |
|---------|---------|-------------|--------------|
| 1       | 2       | 1.0x        | 100%         |
| 2       | 3       | 0.67x       | 33.5%        |
| 3       | 2       | 1.0x        | 33.3%        |

### Тест 2: Пароль довжиною 5 символів

| Воркери | Час (с) | Прискорення | Ефективність |
|---------|---------|-------------|--------------|
| 1       | 204     | 1.0x        | 100%         |
| 2       | 102     | 2.0x        | 100%         |
| 3       | 68      | 3.0x        | 100%         |

## 📈 Аналіз продуктивності <a id="performance-analysis"></a>

### Ключові висновки

#### ✅ Ідеальне масштабування для "важких" завдань (L≥5)

Для обчислювально-складних завдань система PARCS демонструє:
- **Лінійне прискорення**: Час виконання зменшується пропорційно кількості воркерів
- **100% ефективність**: Мінімальні накладні витрати на синхронізацію
- **Ідеальна масштабованість**: S = N (прискорення дорівнює кількості воркерів)

#### ⚠️ Межа ефективності для "легких" завдань (L≤4)

Для швидких завдань спостерігається:
- **Домінація overhead**: Накладні витрати перевищують час обчислень
- **Відсутність прискорення**: Додавання воркерів не покращує продуктивність
- **Можливе сповільнення**: Час виконання може збільшуватися через мережеву комунікацію

### Рекомендації

- **Використовуйте паралелізацію** для завдань з довжиною паролю ≥5 символів
- **Уникайте паралелізації** для швидких завдань (<5 секунд виконання)
- **Оптимальна кількість воркерів** залежить від складності завдання та доступних ресурсів

## 📚 Технічний стек <a id="technical-stack"></a>

- **Мова програмування**: Python 3.x
- **Паралелізація**: PARCS (Pyro4)
- **Хмарна платформа**: Google Cloud Platform
- **Контейнеризація**: Docker
- **Хешування**: MD5 (hashlib)

## 👨‍💻 Автор <a id="author"></a>

**Вяльцев Олександр**  
Київський національний університет імені Тараса Шевченка  
Факультет комп'ютерних наук та кібернетики  
Група ТТП-42

---

*Лабораторна робота №1 з дисципліни "Хмарні обчислення", 2025*
